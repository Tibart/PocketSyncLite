import { describe, it } from 'vitest';

describe('renderer', () => {
  it.todo('renders a recording note with title and body');
  it.todo('includes transcript block when transcript is present');
  it.todo('omits transcript block when transcript is absent');
});
