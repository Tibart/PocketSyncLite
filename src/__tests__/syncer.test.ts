import { describe, it } from 'vitest';

describe('syncer', () => {
  it.todo('creates a new note when recording does not exist in vault');
  it.todo('skips a recording that already exists in vault');
  it.todo('reports sync results with created and skipped counts');
});
